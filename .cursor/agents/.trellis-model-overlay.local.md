# Trellis ephemeral subagent model overlay (local)

**Not a Trellis subagent.** Main session only: apply **one-shot** `model:` to a `trellis-*.md` frontmatter before `Task`, then restore.

| Field | Value |
| --- | --- |
| Active agent | *(none — inherit)* |
| Model ID | *(none)* |
| Set at | — |
| Restore after | dispatch completes |

See `.trellis/spec/guides/cursor-subagent-policy.md` → **Ephemeral model overlay (per dispatch)**.