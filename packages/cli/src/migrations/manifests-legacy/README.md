# Legacy migration manifests (@blxzer/trellis line)

These JSON files supported `trellis update` for the internal **`@blxzer/trellis`** npm package (0.x through 1.1.x).

They are **not shipped** in `@blxzer/cursor-trellis` tarballs. The public product line starts fresh at `../manifests/0.1.0.json`.

Kept in-repo for historical reference and forensic upgrade debugging only.
